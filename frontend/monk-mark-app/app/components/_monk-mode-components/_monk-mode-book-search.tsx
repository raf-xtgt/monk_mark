import React, { useEffect, useRef } from 'react';
import { View, Text, StyleSheet, Animated } from 'react-native';

const MonkModeBookSearch: React.FC = () => {
  
  return (
    <View>
        <Text>book search</Text>
    </View>
  );
};


export default MonkModeBookSearch;
